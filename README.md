# insurance-fraud-detection-sql
End-to-end insurance claims fraud detection system built in MySQL 8.0 — partitioned tables, window functions, recursive CTEs, stored procedures, triggers, and Power BI reporting layer.
